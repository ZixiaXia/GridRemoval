### Train
`events` and `model` are saved in the `./record`.
```bash
python WvSR.py
```

### Test
Enter the path of test files, and the inferred results are saved in the `./result`.
```bash
python DWSRx4.py
```
